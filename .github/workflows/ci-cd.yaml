name: SQLFluff Linting

on:
  push:
    branches:
      - testanother
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install sqlfluff
          cd sqlfluff-plugin-iceberg
          pip install -e .
          pip list | grep sqlfluff-plugin-iceberg

      - name: Verify dialect loading
        run: |
          python -c "from sqlfluff.core import Linter; print(Linter().dialect_registry)"

      - name: Lint SQL files
        run: |
          echo "📄 Linting SQL files..."
          sqlfluff lint sql/ --dialect snowflake_with_iceberg
